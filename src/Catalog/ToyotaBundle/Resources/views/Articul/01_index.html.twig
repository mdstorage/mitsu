{% extends 'CatalogCommonBundle:Articul:01_index.html.twig' %}
{% use 'CatalogToyotaBundle::toyota.base.html.twig' %}
{% block formAction %}
    {% if app.request.get('token') %}
        {{ path('articul_toyota_find_token', {'token': app.request.get('token')}) }}

    {% else %}

        {{ path('articul_toyota_find') }}

    {% endif %}
{% endblock formAction %}

{% block articulScript %}
    <script>
        $("#articul-button").on('click', function(){
            if($("#articul").val(). replace(/\s+/g, '').length < 7){
                alert("Артикул должен содержать не менее 7 знаков!");
                return;
            }
        });
    </script>
{% endblock articulScript %}