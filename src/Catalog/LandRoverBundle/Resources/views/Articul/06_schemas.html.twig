{% extends 'CatalogCommonBundle:Catalog:06_schemas.html.twig' %}
{% use 'CatalogLandRoverBundle::landrover.base.html.twig' %}
{% block breadcrumbs %}
    <a href="{% if  app.request.get('token') %} {{ path('articul_landrover_find_token', parameters) }} {% else %} {{ path('articul_landrover_find') }} {% endif %}">Выбрать модель </a> / <a href="{{ path('articul_landrover_groups', parameters) }}">Выбрать группу запчастей</a> / <a href="{{ path('articul_landrover_subgroups', parameters) }}">Выбрать подгруппу запчастей</a> / Выбрать схему
{% endblock breadcrumbs %}

{% block subGroupRuname %}{{ oContainer.getActiveGroup.getSubGroup(parameters['subGroupCode']).getRuname }}{% endblock subGroupRuname %}


{% block region %}{% endblock region%}
{% block regions %}{% endblock regions %}
{% block complectation %}
{% endblock complectation %}
{% block modification %}
{% endblock modification %}


{% block schema %}
    {% if schema.getCode is not empty %}
        <div style=" height: 320px; " class="col-sm-2 col-sm-offset-5 col-md-2 col-md-offset-0">
            <a href="{{ path('articul_landrover_schema', parameters|merge({'schemaCode': schema.getCode})) }}">



                <div class="row">
                    {{ schema.getRuname|upper }}
                </div>

                {#    {% if schema.getOption('add') != schema.getOption('descr') %}

                        <div class="row">
                            {{ schema.getOption('descr')|convert_encoding('cp1251', 'utf8') }}
                        </div>

                    {% if schema.getOption('add') is not empty %}

                        <div class="row">
                        {{ schema.getOption('add') }}
                        </div>

                    {% endif %}
                        {% else %}
                            <div class="row">
                                {{ schema.getOption('descr')|convert_encoding('UTF-8', 'CP1251')|upper }}
                            </div>


                    {% endif %}
                    #}



                <div class="row">
                    <img src="{{ asset(constant('Catalog\\CommonBundle\\Components\\Constants::PREFIX_STATIC')~'/bundles/cataloglandrover/Images/'~schema.getOption('pictureFolder')~'/'~schema.getCode~'.png') }}" width="150" alt="{{ schema.getRuname }}"/>
                </div>


            </a>
        </div>
    {% endif %}
{% endblock schema %}