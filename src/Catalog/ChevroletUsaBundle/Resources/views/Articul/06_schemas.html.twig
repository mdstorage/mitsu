{% extends 'CatalogCommonBundle:Catalog:06_schemas.html.twig' %}
{% use 'CatalogChevroletUsaBundle::chevroletusa.base.html.twig' %}
{% block breadcrumbs %}
    <a href="{% if  app.request.get('token') %} {{ path('articul_chevroletusa_find_token', parameters) }} {% else %} {{ path('articul_chevroletusa_find') }} {% endif %}">Выбрать регион, модель, модификацию</a> / <a href="{{ path('articul_chevroletusa_groups', parameters) }}">Выбрать группу запчастей</a> /
    Выбрать схему
{% endblock breadcrumbs %}

{% block subGroupRuname %}{{ oContainer.getActiveGroup.getSubGroup(parameters['subGroupCode']).getRuname }}{% endblock subGroupRuname %}

{% block subGroup %}
{% endblock subGroup %}

{% block schema %}
    {% if schema.getCode is not empty %}
        <div style=" height: 300px; " class="col-sm-4 col-sm-offset-5 col-md-4 col-md-offset-0">
            <a href="{{ path('articul_chevroletusa_schema', parameters|merge({'schemaCode': schema.getCode})) }}">

                <div class="row">
                    ({{ schema.getCode }}) {{ schema.getRuname }}
                </div>


                <div class="row">
                    <img src="{{ asset(constant('Catalog\\CommonBundle\\Components\\Constants::PREFIX_STATIC')~'/bundles/cataloggm/Images/G/'~schema.getOption('IMAGE_NAME')~'.gif') }}" width="200" alt="{{ schema.getRuname }}"/>
                </div>


            </a>
        </div>
    {% endif %}
{% endblock schema %}